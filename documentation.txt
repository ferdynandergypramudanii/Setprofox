:'
    DOCUMENTATION CODE
  1. shebang, yang menunjukkan bahwa skrip ini harus dijalankan menggunakan Bash shell. 

  4. FIREFOX_PROFILE_PATH, Variabel ini menyimpan path ke direktori profil Firefox 
  5. $USER_JS_PATH, Variabel yang menyimpan path ke file user.js

    -a, (all) menampilkan file tersembunyi zijgyhp2.default-release

    tambahkan user.js dulu

  12. <<, heredoc operator (heredocument) memberi tahu shell bahwa teks berikutnya adalah input sampai EOT akhir.
      EOT, penanda awal dan akhir blok teks dalam heredoc. 
      >, redirection Operator, output dari perintah cat yang akan ditulis ke file di sebelah kanannya.
      penulisan <<EOT (tanpa jeda) 
  
  13-19. Teks yang berada antara <<EOT dan EOT akan ditulis kedalam file $USER_JS_PATH


  profile firefox menyimpan semua data terkait dengan profil pengguna Firefox seperti konfigurasi, ekstensi, dll
  File user.js adalah file konfigurasi opsional di profil Firefox yang digunakan untuk menetapkan preferensi pengguna secara langsung. 

'
